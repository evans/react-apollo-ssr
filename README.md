To get things working, we need a client side js bundle passed along-side. For a working example, check out [this branch](https://github.com/evans/react-apollo-ssr/tree/reactql)

run:

```bash
npm install
npm start
```
